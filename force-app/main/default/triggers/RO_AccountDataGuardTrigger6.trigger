trigger RO_AccountDataGuardTrigger6 on Account (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    // Training trigger for destructive deployment practice.
}

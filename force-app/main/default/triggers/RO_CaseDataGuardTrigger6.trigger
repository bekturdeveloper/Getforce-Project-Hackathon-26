trigger RO_CaseDataGuardTrigger6 on Case (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    // Training trigger for destructive deployment practice.
}

trigger RO_ContactDataGuardTrigger8 on Contact (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    // Training trigger for destructive deployment practice.
}

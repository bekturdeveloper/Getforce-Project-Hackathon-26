trigger RO_LeadDataGuardTrigger3 on Lead (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    // Training trigger for destructive deployment practice.
}

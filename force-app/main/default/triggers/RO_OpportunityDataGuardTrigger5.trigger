trigger RO_OpportunityDataGuardTrigger5 on Opportunity (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    // Training trigger for destructive deployment practice.
}
